import "../styles.css";
import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box className="header">
      <h1 className="headerTitle">Donations Express & React</h1>
    </Box>
  );
};

export default Header;
